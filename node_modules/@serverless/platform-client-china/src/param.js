'use strict';
const { param } = require('./api');

const paramSet = async (sdk, value) => {
  return await param.paramSet(sdk, value);
};

const paramList = async (sdk, value) => {
  return await param.paramList(sdk, value);
};
module.exports = {
  paramSet,
  paramList,
};
